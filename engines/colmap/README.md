Bundled from the official `colmap-x64-windows-nocuda.zip` release asset. The
application invokes `bin/colmap.exe` directly with explicit CPU feature and
matching flags. The adjacent `plugins/` directory is preserved as published.
